apiVersion: v1
kind: Service
metadata:
  name: simple-jwt-ap
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: simple-jwt-ap
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-jwt-ap
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 2
      maxSurge: 2
  selector:
    matchLabels:
      app: simple-jwt-ap
  template:
    metadata:
      labels:
        app: simple-jwt-ap
    spec:
      containers:
        - name: simple-jwt-ap
          image: CONTAINER_IMAGE
          securityContext:
            privileged: false
            readOnlyRootFilesystem: false
            allowPrivilegeEscalation: false
          ports:
            - containerPort: 8080
